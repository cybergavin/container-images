---
# This workflow is triggered by a schedule and rebuilds all container images
name: Schedule - Build and Test BaseOS Images

on:
  workflow_dispatch:
  schedule:
    - cron: '0 8 * * *'  # Every day at 8 AM UTC

env:
  DEFAULT_REGISTRY: ghcr.io
  IMAGE_NAMESPACE: ${{ github.repository_owner }}

permissions:
  contents: write
  pull-requests: write # For creating PRs
  packages: write      # For pushing test images
  id-token: write      # For cosign OIDC signing
  attestations: write  # For SBOM attachments

jobs:
  discover:
    if: github.event_name != 'schedule' || github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    outputs:
      files: ${{ steps.list.outputs.files }}
    steps:
      - name: Checkout Repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: List Dockerfiles
        id: list
        run: |
          files=$(find . -mindepth 2 -name Dockerfile | jq -R -s -c 'split("\n")[:-1]')
          echo "files=$files" >> $GITHUB_OUTPUT

  build:
    needs: discover
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        dockerfile: ${{ fromJson(needs.discover.outputs.files) }}
    steps:
      - name: Checkout Repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          fetch-depth: 0
    
      - name: Set Variables
        run: |
          file="${{ matrix.dockerfile }}"
          base=$(basename "$(dirname "$file")")
          image="${{ env.DEFAULT_REGISTRY }}/${{ env.IMAGE_NAMESPACE }}/${base}"
          echo "BASE=$base" >> $GITHUB_ENV
          echo "IMAGE=$image" >> $GITHUB_ENV

      - name: Determine BaseOS Version
        run: |
          case "$BASE" in
            amazonlinux) base_img="public.ecr.aws/amazonlinux/amazonlinux:minimal";;
            almalinux)   base_img="almalinux:minimal";;
            alpine)      base_img="alpine";;
            wolfi)       base_img="cgr.dev/chainguard/wolfi-base";;
            *) echo "Unknown: $BASE"; exit 1;;
          esac
          # Exception for Wolfi - free version has no version number
          if [ "$BASE" == "wolfi" ]; then version_id="latest"; else
            version_id=$(docker run --rm "$base_img" awk -F= '/VERSION_ID/ {gsub(/"/,"");print $2}' /etc/os-release)
          fi
          echo "VERSION_ID=$version_id" >> $GITHUB_ENV
          echo "::notice:: Base image: $base_img, Version ID: $version_id"

      - name: Compute Build Tag
        run: |
          DATE=$(date +'%Y.%m')
          count=0
          
          # Try to get existing tags, handle case where repository doesn't exist
          if tag_list=$(skopeo list-tags "docker://${IMAGE}" 2>/dev/null); then
            # Repository exists, count existing tags matching our pattern
            matching_tags=$(echo "$tag_list" | jq -r '.Tags[]?' | grep "^${VERSION_ID}-b.*-${DATE}$" || true)
            if [ -n "$matching_tags" ]; then
              count=$(echo "$matching_tags" | wc -l)
            fi
          else
            echo "Repository ${IMAGE} doesn't exist yet or is inaccessible. Starting with build 01."
          fi
          
          num=$(printf "%02d" $((count + 1)))
          echo "BUILD_TAG=${VERSION_ID}-b${num}-${DATE}" >> $GITHUB_ENV
          echo "::notice:: Computed build tag: ${VERSION_ID}-b${num}-${DATE}"

      - name: Set up Buildx
        uses: docker/setup-buildx-action@e468171a9de216ec08956ac3ada2f0791b6bd435 # v3.11.1

      - name: Lint Dockerfile
        uses: hadolint/hadolint-action@54c9adbab1582c2ef04b2016b760714a4bfde3cf # v3.1.0
        with:
          dockerfile: ${{ matrix.dockerfile }}
          failure-threshold: error
          ignore: DL3018,DL3059,DL3041,DL3006

      - name: Login to Registry
        uses: docker/login-action@74a5d142397b4f367a81961eba4e8cd7edddf772 # v3.4.0
        with:
          registry: ${{ env.DEFAULT_REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GHCR_PAT }}

      - name: Extract metadata
        id: docker_meta
        uses: docker/metadata-action@902fa8ec7d6ecbf8d84d538b9b233a880e428804 # v5.7.0
        with:
          images: ${{ env.IMAGE }}
          tags: |
            type=raw,value=${{ env.BUILD_TAG }}
            type=raw,value=latest

      - name: Build & Push Test Image, generate SBOM & Provenance
        id: build-and-push
        uses: docker/build-push-action@263435318d21b8e681c14492fe198d362a7d2c83 # v6.18.0
        env:
          DOCKER_BUILD_SUMMARY: false  # Explicitly disable build reports/artifacts
        with:
          context: .
          file: ${{ matrix.dockerfile }}
          push: true
          tags: ${{ steps.docker_meta.outputs.tags }}
          sbom: true
          provenance: mode=max
          outputs: type=image,name=${{ env.IMAGE }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Extract and Store SBOM
        run: |
          docker buildx imagetools inspect ${{ env.IMAGE }}:${{ env.BUILD_TAG }} --format "{{ json .SBOM }}" > sbom-${{ env.BASE }}-${{ env.BUILD_TAG }}.json


      - name: Test Image Functionality
        shell: bash
        run: |
          set -euo pipefail
          echo "Starting testing for container image ${{ env.IMAGE }}:${{ env.BUILD_TAG }}"

          # Test container startup
          echo "::group::Container Startup Test"
          if output=$(docker run --rm ${{ env.IMAGE }}:${{ env.BUILD_TAG }} echo "Container startup test passed" 2>&1); then
            echo "::notice:: SUCCESS: Container startup test passed"
            echo "Output: $output"
          fi
          echo "::endgroup::"
          
          # Test upgrade functionality - should not be allowed
          echo "::group::Upgrade Test"
          case "${{ env.BASE }}" in
            amazonlinux|almalinux) if docker run --rm ${{ env.IMAGE }}:${{ env.BUILD_TAG }} microdnf upgrade -y; then
                                      echo "::error:: FAILURE: Upgrade test should have failed, but passed"
                                      exit 1
                                    else
                                      echo "::notice:: SUCCESS: Upgrade test correctly failed"
                                    fi ;;
            alpine|wolfi) if docker run --rm ${{ env.IMAGE }}:${{ env.BUILD_TAG }} apk upgrade; then
                      echo "::error:: FAILURE: Upgrade test should have failed, but passed"
                      exit 1
                    else
                      echo "::notice:: SUCCESS: Upgrade test correctly failed"
                    fi ;;
          esac
          echo "::endgroup::"

          # Test existence of appuser
          echo "::group::App User Test"
          if docker run --rm ${{ env.IMAGE }}:${{ env.BUILD_TAG }} id appuser &>/dev/null; then
            echo "::notice:: SUCCESS: App user exists"
          fi
          echo "::endgroup::"
          echo "::notice:: ✅ Functionality tests passed"

      - name: Scan with Trivy
        uses: aquasecurity/trivy-action@dc5a429b52fcf669ce959baa2c2dd26090d2a6c4 # v0.32.0
        with:
          image-ref: ${{ env.IMAGE }}:${{ env.BUILD_TAG }}
          format: json
          output: trivy-${{ env.BASE }}.json
          ignore-unfixed: true
          severity: 'CRITICAL,HIGH,MEDIUM'
          vuln-type: 'os,library'
      
      - name: Install Cosign
        uses: sigstore/cosign-installer@398d4b0eeef1380460a10c8013a76f728fb906ac # v3.9.1

      - name: Sign and verify test images with GitHub OIDC Token
        env:
          DIGEST: ${{ steps.build-and-push.outputs.digest }}
          TAGS: ${{ steps.docker_meta.outputs.tags }}
          COSIGN_EXPERIMENTAL: 1  # Enable keyless signing
        shell: bash
        run: |
          set -euo pipefail
          echo "::group:: 🔐 Signing images with GitHub OIDC (keyless)..."
          # Sign using OIDC token (non-interactive)
          for tag in ${TAGS}; do
            echo "Signing: ${tag}@${DIGEST}"
            cosign sign --yes "${tag}@${DIGEST}"
          done
          echo "::notice:: ✅ Images signed successfully"
          echo "::endgroup::"
          
          echo "::group::🔍 Verification of signatures, provenance and SBOM"
          for tag in ${TAGS}; do
            echo "Verifying: ${tag}@${DIGEST}"
            cosign verify "${tag}@${DIGEST}" \
              --certificate-identity-regexp="^https://github.com/${{ github.repository }}/" \
              --certificate-oidc-issuer=https://token.actions.githubusercontent.com
            cosign verify-attestation "${tag}@${DIGEST}" \
              --certificate-identity-regexp="^https://github.com/${{ github.repository }}/" \
              --certificate-oidc-issuer=https://token.actions.githubusercontent.com --type slsaprovenance
            cosign verify-attestation "${tag}@${DIGEST}" \
              --certificate-identity-regexp="^https://github.com/${{ github.repository }}/" \
              --certificate-oidc-issuer=https://token.actions.githubusercontent.com --type spdx
          done
          echo "::notice:: ✅ All tags passed signature, provenance, and SBOM verification."
          echo "::endgroup::"
